#include <iostream>
#include <string>

using namespace std;

int main() {
	string input, brack;
	brack = "()[] {}";
	int len, i;
	cin >> input;
	len = input.length();
	cout << len;
	for (i = 0; i < len; i ++) {
		if (input[i].contains(brack)) {
			cout << i;
		}
	}
		
		
	
}
